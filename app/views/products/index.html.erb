<h1><%= t ".title" %></h1>


<%= link_to "Login", new_product_path unless authenticated? %>

<%= link_to "Create", new_product_path if authenticated? %>


<div id="products">
  <% @products.each do |product| %>
    <div>
      <%= link_to product.name, product %>
    </div>
  <% end %>
</div>
